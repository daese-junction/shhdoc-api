# 협업 규칙

2박 3일 해커톤 프로젝트입니다. **속도 > 완성도.**
아래 규칙은 3일 뒤 폐기 대상이고, 지키기 어려우면 단톡에서 바꿉니다.

스택: Java 21 / Spring Boot 4.1 / Gradle / MySQL 8.4 / JPA

---

## 1. API 명세가 유일한 계약

코드보다 명세를 먼저 합의합니다. 엔드포인트 이름 + 요청/응답 JSON.

- 합의된 명세를 **혼자 바꾸지 않습니다.** 바꿔야 하면 단톡에 먼저 올립니다.
- 명세는 **한 곳에만** 둡니다. 두 곳에 두면 반드시 갈라집니다.
- 프론트가 있다면 명세 합의 전에는 아무도 코드를 짜지 않습니다.

> 명세 위치: _(회의에서 정하고 여기 링크 추가)_

## 2. 이슈

- **반나절 이상 걸리는 작업만** 이슈로 만듭니다. 30분짜리는 그냥 합니다.
- 제목은 하고 싶은 말 그대로. 본문 템플릿 없습니다.
- 라벨은 3개만: `feat` / `bug` / `deploy`
- 이슈를 시작할 때 **본인을 assignee로 지정**합니다. 이게 "내가 지금 이걸 하고 있다"는 유일한 신호입니다.
- 이슈 번호는 브랜치와 PR에서 재사용합니다 (3, 4번 참고).

## 3. 브랜치

- `main` + 작업 브랜치만 사용합니다. develop, release 브랜치 없습니다.
- 브랜치 이름: `feat/<이슈번호>-<짧은설명>` (예: `feat/12-attachment-upload`)
  이슈가 없으면 `feat/attachment-upload`.
- **main에 직접 푸시하지 않습니다.** (아래 딱 하나 예외: 첫날 초기 세팅 커밋)
- **main은 항상 실행 가능해야 합니다.** 깨뜨렸으면 즉시 단톡에 알립니다.

## 4. PR과 머지

- PR은 **만듭니다.** 다만 **리뷰를 기다리지 않고 셀프 머지**합니다. 리뷰 대기 시간이 제일 비쌉니다.
- PR 본문에 `Closes #12` 한 줄. 머지되면 이슈가 자동으로 닫힙니다.
- **Squash merge로 통일**합니다. 히스토리가 PR 단위로 남아서 되돌리기가 쉽습니다.
- 머지 전 `git pull --rebase origin main` → 로컬에서 충돌 해결 → 푸시 → 머지.
- **하루 최소 한 번은 main에 머지합니다.** 3일차에 한 번에 합치면 죽습니다.
- 로직이 애매해서 눈이 하나 더 필요하면 PR에 `@이름` 멘션하되, **답을 기다리며 멈추지 않습니다.**
  일단 머지하고 코멘트가 달리면 그때 고칩니다.

## 5. 커밋

- 메시지 형식 강제 없습니다. 대신 **자주** 커밋합니다.
- 커밋 단위로 되돌릴 수 있으면 충분합니다. 어차피 squash로 합쳐집니다.

## 6. 테스트

**AI로 작성하되, 읽지 않고 머지하지 않습니다.** 이 규칙의 전부입니다.

- **단위 테스트** — Service의 분기/조건 로직. `@ExtendWith(MockitoExtension.class)`, DB 안 띄웁니다.
- **통합 테스트** — Controller에서 DB까지. `@SpringBootTest` + `@Import(TestcontainersConfiguration.class)`.
  도메인당 **핵심 플로우 1~2개면 충분**합니다. 전 경로를 덮으려 하지 않습니다.
- **PR 머지 전 `./gradlew test` 통과가 조건입니다.** 깨진 채로 머지 금지.
- getter/setter, 단순 위임 메서드는 테스트하지 않습니다.

AI에게 시킬 때:

- "이 서비스 테스트 짜줘" ❌ → **"이 메서드가 실패하는 경우를 짜줘"** ⭕
  성공 케이스만 있는 테스트는 아무것도 못 막습니다.
- **AI가 짠 테스트가 실패하면 테스트부터 고치지 말고 코드를 먼저 의심합니다.**
  반대로 하면 테스트가 장식이 됩니다.
- AI는 assertion 값을 그럴듯하게 지어냅니다. **기댓값이 맞는지는 사람이 확인합니다.**

```bash
./gradlew test    # Testcontainers가 MySQL을 자동으로 띄웠다 내립니다
```

Docker Desktop만 켜져 있으면 되고, 로컬 `compose.yaml` DB와 섞이지 않습니다.

## 7. 담당 영역

- 도메인 하나에 담당자 한 명. 남의 도메인 파일은 건드리지 않습니다.
- 남의 코드 수정이 필요하면 직접 고치지 말고 담당자에게 말합니다.
- **공용 파일을 고쳤으면 단톡에 한 줄 공유합니다.**
  `build.gradle`, `application.yml`, `compose.yaml`, 공통 예외 클래스.
- `deploy/` 는 배포 담당자 영역입니다. 나머지는 건드리지 않습니다.

> 담당표: _(회의에서 정하고 여기 추가)_

## 8. 시크릿

- API 키, DB 비밀번호는 **환경변수로만.** yml 하드코딩 금지.
- `.env`, `application-local.yml` 은 이미 `.gitignore` 에 있습니다.
- 키가 커밋됐으면 즉시 알리고 **재발급**합니다. "나중에 지우자" 금지 — 히스토리에 남습니다.

## 9. 로컬 환경

```bash
./gradlew bootRun     # MySQL 컨테이너가 자동으로 뜹니다
curl localhost:8080/ping   # → ok
```

- `spring-boot-docker-compose` 가 `compose.yaml` 을 읽어 MySQL을 띄우고 접속 정보까지 주입합니다.
  Docker Desktop만 켜두면 되고, MySQL을 로컬에 설치하지 않습니다.
- 스키마는 `ddl-auto: update`. 마이그레이션 툴 안 씁니다.
- 단, **컬럼을 삭제하거나 타입을 바꿀 땐 단톡에 공유합니다.** `update` 로는 반영이 안 돼서
  본인 로컬만 멀쩡하고 남들 로컬이 깨집니다. 그럴 땐 각자 `docker compose down -v` 로 날리고 다시 띄웁니다.

## 10. 막혔을 때

- **20분 막히면 무조건 물어봅니다.** 혼자 파는 게 팀 전체에서 제일 비쌉니다.
- 그래도 안 되면 하드코딩하고 `// TODO:` 남기고 넘어갑니다.
  **데모에 없는 기능은 0점입니다.**

## 11. 안 하는 것

리뷰 승인 대기 · DB 마이그레이션 툴 · 커밋 컨벤션 · CI · 이슈/PR 템플릿 · 로깅 설정 · API 문서 자동화 · 커버리지 목표치

이걸 하고 싶어지면 "데모까지 남은 시간"을 먼저 봅니다.

---

## 코드 규칙 (최소)

- 레이어는 **Controller → Service → Repository** 3개뿐. Facade, Manager, Mapper 만들지 않습니다.
- 컨트롤러에 비즈니스 로직을 넣지 않습니다. 서비스 호출만.
- DTO는 `record`.
- 인터페이스는 구현체가 2개 이상일 때만. `XxxService` + `XxxServiceImpl` 금지.
- 응답 래퍼(`ApiResponse<T>`) 만들지 않습니다. DTO를 그대로 반환하고, 프론트가 요구하면 그때 추가.
- 예외는 공통 예외 하나 + `@RestControllerAdvice` 하나. ErrorCode enum 금지.

> 패키지 구조: _(회의에서 정하고 여기 추가)_
